// const controllers = require('../controllers');

// const Router = require('express').Router();

// Router.get('/webhook', controllers.ChatBot.authChatBot);

// Router.post('/webhook', controllers.ChatBot.postChatBot);

// module.exports = Router;
